[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "osrparse>=7.0.1",
    "python-dateutil>=2.9.0.post0",
    "slider>=0.8.3",
]
